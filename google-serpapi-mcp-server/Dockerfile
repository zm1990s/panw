# Generated by https://smithery.ai. See: https://smithery.ai/docs/build/project-config
FROM python:3.13-slim

WORKDIR /app

# Upgrade pip and install dependencies
RUN pip install --no-cache-dir --upgrade pip && \
    pip install --no-cache-dir mcp[cli]>=1.3.0 google-search-results>=2.4.2 python-dotenv

# Copy entire project into container
COPY . /app

# Ensure Python outputs are unbuffered
ENV PYTHONUNBUFFERED=1

# Default command is defined by smithery; we just ensure files exist
CMD ["python", "src/serpapi-mcp-server/server.py"]
